services:
  - type: worker
    name: agency-os-bot
    env: docker
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: AIRTABLE_API_KEY
        sync: false
      - key: AIRTABLE_BASE_ID
        sync: false 